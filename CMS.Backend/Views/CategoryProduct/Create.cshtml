@{
    Layout = "_LayoutAdmin";
    ViewData["Title"] = "Thêm Danh mục Sản phẩm";
}
@model CMS.Data.Entities.CategoryProduct

<div class="mb-4">
    <h2 class="text-primary font-weight-bold">
        <i class="bi bi-tag-fill"></i> THÊM DANH MỤC SẢN PHẨM MỚI
    </h2>
    <p class="text-muted">Tạo danh mục sản phẩm thời trang mới trong cơ sở dữ liệu.</p>
</div>

<div class="row">
    <div class="col-md-8 col-lg-6">
        <div class="card shadow-sm border-0">
            <div class="card-body p-4">
                <form asp-action="Create" method="post">
                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                    <div class="mb-3">
                        <label asp-for="Name" class="form-label">Tên danh mục sản phẩm *</label>
                        <input asp-for="Name" class="form-control" placeholder="Nhập tên danh mục (Ví dụ: Váy dạ hội, Đồ công sở...)" required />
                        <span asp-validation-for="Name" class="text-danger small"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Description" class="form-label">Mô tả danh mục</label>
                        <textarea asp-for="Description" class="form-control" rows="4" placeholder="Nhập mô tả ngắn gọn về danh mục sản phẩm này..."></textarea>
                        <span asp-validation-for="Description" class="text-danger small"></span>
                    </div>

                    <div class="d-flex justify-content-end gap-2 border-top pt-3 mt-4">
                        <a asp-action="Index" class="btn btn-outline-secondary">
                            <i class="bi bi-x-circle"></i> Hủy bỏ
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-check-circle"></i> Lưu vào hệ thống
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
